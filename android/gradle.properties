# Reduce memory to prevent JVM crashes
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Force Gradle to use Java 17
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.15.6-hotspot

# Disable Gradle daemon to prevent JVM crashes
org.gradle.daemon=false

# AndroidX support
android.useAndroidX=true
android.enableJetifier=true
